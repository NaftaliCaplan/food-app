# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Docs

`docs/ARCHITECTURE.md` maps how the app's pieces fit together (what's where, how a request flows end to end). `docs/decisions/` has the ADRs explaining why specific choices were made. Check both before assuming context about this codebase.
